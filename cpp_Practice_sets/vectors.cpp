#include <iostream>
#include <vector>
using namespace std;
int main()
{
    // vector<int> numbers;
    // int value;
    // for (int i = 0; i < 3; i++)
    // {
    //     cout << "enter number " << endl;
    //     cin >> value;
    //     numbers.push_back(value);
    // }

    // numbers.push_back(3);
    // numbers.push_back(2);
    // numbers.push_back(2);
    // numbers.push_back(6);
    // numbers.push_back(5);

    // cout << "size of the numbers of vectors is " << numbers.size() <<endl;
    // cout << "capacity of the numbers of vectors is " << numbers.capacity();

    // vector <int> num (10,5); // this line means make vector include 10 times 5
    // for (int i = 0; i < num.size(); i++)
    // {
    //     cout<<num[i]<<endl;
    // }
// using fill func

    // vector<int> num(10);
    // fill(num.begin(),num.end(),5);
    // for (int i = 0; i < num.size(); i++)
    //     {
    //         cout<<num[i]<<endl;
    //     }

        // vector <int> num = {3,4,2,1,5,4,6,7,5,4,7,5,4};
        // int count = 0;
        // for (auto &&i : num)
        // {
        //     if (i%3==0 || i %5== 0)
        //     {
        //         count++;
        //     }

        // }
        // cout << "the num divisibale " << count <<endl;
        return 0;
}